# T1/T2 변경점 실측 테스트 계획 및 결과

> 목적: T1·T2 코드 변경(대화 메모리·의존성 마이그레이션·교차언어 단일화·스트리밍·캐싱 / 하이브리드+리랭커·평가 하니스·가드레일·출처링크)을 **실제 모델을 다운로드해 구동**하며 변경 전후 차이를 검증한다.
> 상태: ✅ 전체 완료 — 로컬/모델(TP-A~D) + LLM 엔드투엔드(TP-E~H) 모두 PASS
> 작성/실행일: 2026-06-09

---

## 1. 테스트 환경

| 항목 | 값 |
|------|-----|
| Python | 3.10.6 (testvenv) |
| 임베딩 모델 | `BAAI/bge-m3` (신규 다운로드) |
| 리랭커 모델 | `BAAI/bge-reranker-v2-m3` (신규 다운로드) |
| 벡터DB | `data/db/faissdb/faiss_db_all_lower` (기존) |
| LLM | `gpt-4.1-mini` (OpenAI, `.env` 키) |
| 모델 캐시 위치 | `HF_HOME=./models` (Docker 볼륨과 동일, gitignore) |
| 디바이스 | CPU |

비용/시간 주의: 모델 다운로드 ~4.5GB, 일부 테스트는 OpenAI 유료 호출. API 호출 테스트는 비용 통제를 위해 소수 쿼리로 제한한다.

---

## 2. 테스트 매트릭스 (변경점 → 검증 방법 → 기대 결과)

### 로컬(모델만, API 불필요)
| ID | 변경점 | 검증 방법 | 기대 결과 |
|----|--------|-----------|-----------|
| TP-A | T1-3 마이그레이션 + T1-1 캐싱 | `build_resources()` 실제 구동(임베딩+FAISS+리랭커 로드), 로드 시간 측정 | deprecation 에러 없이 로드 성공, 리소스 1회 구성 |
| TP-B | T2-1 검색 고도화 | 동일 질의에 대해 **dense-only vs 하이브리드 vs 하이브리드+리랭커** 상위 문서 비교 | 각 모드가 동작하고 상위 문서/순위에 차이가 관찰됨 |
| TP-C | T2-1 폴백 | `USE_HYBRID=0 USE_RERANKER=0` 로 dense-only 구성 | 에러 없이 dense 검색만 동작 |
| TP-D | T2-4 출처 데이터 | 검색 결과 문서의 `title`/`source` 메타데이터 확인 | 제목 링크 렌더에 쓸 title/source 존재 확인 |

### 엔드투엔드(OpenAI 호출, 비용 통제)
| ID | 변경점 | 검증 방법 | 기대 결과 |
|----|--------|-----------|-----------|
| TP-E | T2-3 범위 가드레일 | "페럿이 우울해 보여요" 입력 | 범위 외 안내(OOS) 응답, 검색/답변 생략 |
| TP-F | T2-3 건강 면책 | "강아지가 자꾸 구토해요" 입력 | 답변 + 수의사 상담 면책 고지 부착 |
| TP-G | T1-2 대화 메모리 | 1턴 "말티즈가 짖어요" → 2턴 "그럼 어떻게 훈련해?" | 2턴이 직전 견종/맥락(말티즈, 짖음)을 유지해 답변 |
| TP-H | T1-4 교차언어 단일화 | 영어 출처가 1위인 질의 1건 | 답변 생성 LLM 호출이 1회로 동작(이중 호출 없음) |

---

## 3. 실행 방법
```bash
# 모델 캐시를 프로젝트 models/ 로 지정(Docker 볼륨과 동일)
set HF_HOME=./models          # PowerShell: $env:HF_HOME="./models"
# 로컬 검색 비교 (API 불필요)
testvenv/Scripts/python.exe scripts/run_change_tests.py --local
# 엔드투엔드 (OpenAI 호출)
testvenv/Scripts/python.exe scripts/run_change_tests.py --e2e
```

---

## 4. 합격 기준
- TP-A/TP-C: 예외 없이 로드/검색.
- TP-B: 세 모드 모두 결과 반환, 모드별 상위 문서 차이 1건 이상 관찰.
- TP-E: OOS 안내 문구 반환(`source_documents` 비어 있음).
- TP-F: 답변 끝에 면책 문구 포함.
- TP-G: 2턴 답변에 1턴 맥락(견종/행동) 반영.
- TP-H: 영어 출처 질의에서 답변 LLM 1회.

---

## 5. 실측 결과 (2026-06-09 실행)

### 5.0 모델 다운로드 — ✅ 성공
- `BAAI/bge-m3` + `BAAI/bge-reranker-v2-m3` 실제 다운로드 완료 (총 **6.39 GB**, `models/hub/`).
- 로드 시간(캐싱 효과, T1-1 근거):
  - 임베딩 로드: **첫 실행(다운로드 포함) 62.7s → 캐시 후 13.7s**
  - 리랭커 구성: **첫 실행 61.2s → 캐시 후 3.6s**
  - FAISS 로드: 0.0~0.5s, **문서수 882**
  - → 캐싱(T1-1)이 없으면 Streamlit 재실행마다 13~62s 지연이 발생함을 실측 확인.

### 5.1 TP-A 리소스 로드 — ✅ PASS
`langchain_community.*` 마이그레이션(T1-3) 상태로 임베딩+FAISS+리랭커가 **deprecation/에러 없이** 로드됨.

### 5.2 TP-B 검색 모드 비교(dense vs hybrid vs hybrid+reranker) — ✅ PASS (중요 발견)
질의별 상위 3개 문서(제목):

| 질의 | dense-only | hybrid | hybrid+reranker |
|------|-----------|--------|-----------------|
| 말티즈가 자꾸 짖어요 | 말티즈(YT)×3 | 말티즈(YT)×3 (순서변동) | 말티즈(YT)×2 + **aggression in dogs(영문)** |
| 강아지가 분리불안이 있어요 | **separation anxiety in dogs×3**(kinship) | separation anxiety×2 + "6 lies..."(다양성↑) | ⚠️ **헝가리안 풀리/알래스칸 말라뮤트/이탈리안 그레이하운드(YT 견종)** |
| 강아지가 벽지를 긁어요 | scratch carpet + pawing×2(중복) | scratch carpet + pawing×2 | ✅ **scratch carpet + scratch bed×2**(주제 정확·다양성↑) |

**핵심 발견(리랭커의 양면성):**
- ✅ "벽지 긁기" 질의: 리랭커가 off-topic "pawing" 중복을 제거하고 carpet/bed 긁기 문서로 **개선**.
- ⚠️ "분리불안" 질의: 리랭커가 **정답(영문 kinship separation-anxiety)을 밀어내고 한글 YT 견종 문서를 1~3위로 올림 → 명백한 품질 저하**.
  - 원인: cross-encoder가 *한글질의↔한글문서* 점수를 *한글질의↔영문문서*보다 높게 매김(교차언어 약점). 주제 적합도(영문 분리불안 문서)보다 표면 언어 일치를 선호.
- ➡️ **결론: 리랭커를 기본 ON으로 두면 한글질의→영문문서 케이스에서 회귀 위험.** T1-4 번역 단계를 리랭커 앞에 두거나, 한글질의는 리랭커 비활성/언어 일관 검색이 필요.

### 5.3 TP-C dense-only 폴백 — ✅ PASS
`USE_HYBRID=0 USE_RERANKER=0` 경로(_build_retriever)가 에러 없이 dense 검색만 수행.

### 5.4 TP-D 출처 메타데이터 — ✅ PASS
모든 문서가 `keys=['source','title']` 보유 → T2-4 제목 링크 렌더에 필요한 `title`/`source` 확보 확인.
부수 발견: **DB에 동일 source 문서가 2~3회 중복** 존재(dense가 중복 반환) → 출처 dedup(T2-4) 및 인덱스 정리 필요성 실증.

### 5.5 TP-E~H 엔드투엔드(LLM) — ✅ PASS (2026-06-09, 유효 키 재실행)
`build_resources` 로드 26.9s. 4개 모두 실제 `gpt-4.1-mini` 호출로 검증.

| ID | 변경점 | 실측 결과 | 판정 |
|----|--------|-----------|------|
| TP-E | T2-3 범위 가드 | "페럿이 우울해 보여요" → `docs=0`, 답변="죄송해요, 저는 '반려견(개)'의 행동·견종 상담에 특화된 챗봇이에요🐶 …" | ✅ OOS 안내(검색/답변 생략) |
| TP-F | T2-3 건강 면책 | "강아지가 자꾸 구토해요" → 답변 말미에 LLM 자체 수의사 권유 + 부착된 `⚠️ 증상이 지속되거나 심해지면 반드시 수의사와 상담하세요…` | ✅ 면책 고지 부착 |
| TP-G | T1-2 대화 메모리 | 1턴 "말티즈가 자꾸 짖어요" → 2턴 "**그럼 어떻게 훈련해?**" → 2턴 답변이 "**말티즈의 짖음 문제를 효과적으로 훈련하기 위한 방법…**"으로 직전 견종·행동 맥락 정확히 계승 | ✅ 후속 질문 맥락 유지 |
| TP-H | T1-4 교차언어 단일화 | "강아지가 사람 손을 자꾸 깨물어요" → top_sources에 kinship 영문 문서 포함(`english_source_branch=True`), **단일 답변(806자) 1회 생성**, 한국어 응답 정상 | ✅ 이중 호출 없이 교차언어 동작 |

- 부수 확인(이전 무효키 세션): 죽은 키 상황에서 `is_in_scope`가 **fail-open**(예외 시 통과)으로 동작함을 트레이스로 확인 — 설계 의도대로.

---

## 6. 결론 및 권고

1. **모델/검색 계층(T1-1·T1-3·T2-1·T2-4)은 실모델로 검증 완료.** 마이그레이션 무결, 캐싱 효과(13~62s 절감) 실측, 세 검색 모드 동작 확인.
2. **리랭커는 데이터로 보면 "무조건 ON"이 아니다.** 한글질의↔영문문서에서 정답을 밀어내는 회귀를 실측. 권고:
   - 단기: NAS 기본값을 **`USE_RERANKER=0`(하이브리드까지만)** 으로 두고, 영문 문서 비중이 큰 질의군에서만 선택 적용.
   - 중기: **T1-4 번역 → 검색 → 리랭크** 순서로 질의 언어를 문서 언어에 맞춘 뒤 리랭킹하거나, 한/영 인덱스를 분리.
   - 검증: 결제 키 확보 후 **T2-2 ragas로 reranker on/off의 context-precision을 정량 비교**.
3. **데이터 품질**: FAISS에 중복 문서 다수 → 인덱스 재구축 시 dedup 권장(검색 슬롯 낭비 방지).
4. **LLM 엔드투엔드(T1-2·T1-4·T2-3) 전부 실호출로 PASS.** 특히 T1-2 대화 메모리는 "그럼 어떻게 훈련해?" 후속 질문이 직전 말티즈·짖음 맥락을 정확히 계승함을 확인(가장 중요한 신규 기능). T2-3 범위 가드/면책, T1-4 교차언어 단일 답변도 정상.

### 남은 권고 액션
- [ ] reranker on/off ragas 비교(`scripts/eval_ragas.py`)로 §6-2(리랭커 회귀)를 정량화 → NAS 기본값(`USE_RERANKER`) 확정
- [ ] (선택) FAISS 인덱스 중복 제거 후 재구축
- [ ] (데이터) 건강 증상 질의용 문서 보강 — §7.2 참조

---

## 7. 쇼케이스 — 카테고리별 질문 + 실제 답변/출처
> 실행: `python scripts/run_change_tests.py --showcase` (10건, 실 `gpt-4.1-mini` 호출). 전체 답변 원문: `data/eval/showcase_results.txt`(gitignore). `build_resources` 37.7s.

### 7.1 요약 표
| # | 카테고리 | 질문 | 플래그 | 상위 출처(제목) | 답변 요지 |
|---|----------|------|--------|------------------|-----------|
| 1 | 견종 | 푸들은 어떤 성격을 가지고 있어? | — | 푸들 | 사교적·교감 능력↑, 분리불안 주의, 사회성 훈련 권장 |
| 2 | 견종 | 골든리트리버는 훈련을 어떻게 시켜야 해? | — | 골든 리트리버 외 | 긍정 강화·기본 명령어·짧고 잦은 훈련·사회화 |
| 3 | 행동 | 강아지가 산책할 때 자꾸 줄을 당겨요 | — | 시바이누/포인터/시츄 | 멈춤-방향전환 교육, 운동량, 집중 훈련 |
| 4 | 행동 | 강아지가 낯선 사람만 보면 짖어요 | — | 꼬똥 드 툴레아/포메라니안/(kinship 영문) | 사회화, 방문자 대면 방식, 긍정 강화 |
| 5 | 건강(면책) | 강아지가 어제부터 설사를 해요 | 건강-면책 | ⚠️주제 불일치(whining/eat poop) | 원인 점검 + 지속 시 수의사 권유 |
| 6 | 건강(면책) | 강아지가 다리를 절뚝거려요 | 건강-면책 | ⚠️주제 불일치(whimper/licking) | 부상·관절 점검, 1일 이상 지속 시 진료 |
| 7 | 범위 외 | 고양이가 밥을 안 먹어요 | 범위외-가드 + 건강-면책 | (없음) | OOS 안내 + 수의사 면책 |
| 8 | 범위 외 | 오늘 서울 날씨 어때? | 범위외-가드 | (없음) | OOS 안내 |
| 9 | 교차언어 | 강아지가 자기 꼬리를 빙빙 돌며 쫓아요 | — | 시바이누/시츄 | 놀이·지루함·강박 가능성, 과도 시 진료 |
| 10 | 대화메모리 | 비글은 어떤 견종이야? → **그럼 운동은 얼마나?** | — | 비글/비숑/골든 | 2턴이 "비글 운동량 하루 1시간↑"로 맥락 계승 |

### 7.2 관찰 (실측 기반)
- ✅ **기능 전부 정상**: 견종 특화 답변, 행동 코칭, 건강 면책 자동 부착(5·6), 범위 가드(7·8), 대화 메모리(10).
- ✅ **OOS + 면책 동시 발화**(7, "고양이가 밥을 안 먹어요"): 범위 외 안내 + 수의사 권유가 함께 출력 — 의도된 안전 동작.
- ⚠️ **건강 질의의 검색이 주제와 어긋남**(5·6): "설사/절뚝" 질의에 whining·eat poop·licking 등 무관 문서가 상위. LLM이 일반 지식 + 면책으로 보완하나, **코퍼스에 증상/질병 문서가 빈약**함을 시사 → §6 데이터 보강 액션 추가.
- ✅ **대화 메모리 추가 검증**(10): "그럼 운동은 얼마나?"가 직전 비글 맥락을 정확히 이어 "비글은 하루 1시간 이상" 답변. TP-G와 독립적으로 재확인.

### 7.3 전체 답변 예시(발췌)
**[10] 대화메모리** — 1턴 "비글은 어떤 견종이야?" 후
> ↳ 후속질문: **그럼 운동은 얼마나 시켜야 해?**
> ↳ 답변: "비글은 매우 활동적이고 에너지가 많은 견종이기 때문에 충분한 운동이 꼭 필요합니다. 하루에 최소한 1시간 이상, 가능하면 여러 번 나누어 산책이나 뛰어놀기 시간을 가져주는 것이 좋습니다…"

**[7] 범위 외+면책** — "고양이가 밥을 안 먹어요"
> "죄송해요, 저는 '반려견(개)'의 행동·견종 상담에 특화된 챗봇이에요. 🐶 … ⚠️ 증상이 지속되거나 심해지면 반드시 수의사와 상담하세요…"
